@extends('admin.layouts.app')
@section('admin-title')<h1>Узлы — Редактировать #{{ $item->id }}</h1>@endsection
@section('admin-content')
<form method="post" action="{{ route('admin.nodes.update', $item) }}" class="form-row">
  @method('PUT')
  @include('admin.nodes._form', ['item'=>$item, 'locations'=>$locations])
</form>
<form method="post" action="{{ route('admin.nodes.destroy', $item) }}" onsubmit="return confirm('Удалить узел?');" style="margin-top:10px">
  @csrf @method('DELETE')
  <button class="btn btn-danger" type="submit">{{ __('Delete') }}</button>
</form>
@endsection

<p style="margin-top:10px"><a class="btn" href="{{ route('admin.nodes.onboarding', $item) }}">Страница подключения</a> <form method="post" action="{{ route('admin.nodes.ping', $item ?? $it) }}" style="display:inline">@csrf <button class="btn btn-sm" type="submit">Проверить подключение</button></form></p>
