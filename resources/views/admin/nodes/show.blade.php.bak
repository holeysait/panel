@extends('admin.layouts.app')
@section('admin-title')<h1>Узел — {{ $item->name }}</h1>@endsection
@section('admin-content')
<div class="card"><div class="card-body">
  <dl class="grid" style="display:grid;grid-template-columns: 200px 1fr;gap:8px">
    <dt>ID</dt><dd>{{ $item->id }}</dd>
    <dt>Локация</dt><dd>{{ optional($item->location)->name ?? '-' }}</dd>
    <dt>FQDN</dt><dd>{{ $item->public_fqdn }}</dd>
    <dt>Daemon URL</dt><dd>{{ $item->daemon_url }}</dd>
    <dt>Статус</dt><dd>{{ $item->status }}</dd>
    <dt>Capabilities</dt><dd><pre style="white-space:pre-wrap">{{ json_encode($item->capabilities, JSON_PRETTY_PRINT|JSON_UNESCAPED_UNICODE) }}</pre></dd>
  </dl>
  <div class="form-actions" style="margin-top:12px">
    <a class="btn btn-primary" href="{{ route('admin.nodes.edit', $item) }}">{{ __('Edit') }}</a>
    <a class="btn" href="{{ route('admin.nodes.index') }}">{{ __('Back') }}</a>
  </div>
</div></div>
@endsection

<p style="margin-top:10px"><a class="btn" href="{{ route('admin.nodes.onboarding', $item) }}">Страница подключения</a> <form method="post" action="{{ route('admin.nodes.ping', $item ?? $it) }}" style="display:inline">@csrf <button class="btn btn-sm" type="submit">Проверить подключение</button></form></p>
